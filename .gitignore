__pycache__/
*.pyc
.DS_Store
*.egg-info/
.env

# Raw Cricsheet downloads — regenerate with `python ingest.py`
data/raw/

# Per-competition and combined CSVs — too large for git; regenerated from zips
data/*.csv
# Parquet sidecar cache — auto-built from CSV on first analyzer load
data/*.parquet

# Word doc backup
~$*.docx
